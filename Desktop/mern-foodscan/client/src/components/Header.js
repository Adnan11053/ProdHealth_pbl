import React from 'react'; import { Link, useNavigate } from 'react-router-dom'; export default function Header(){ const name = localStorage.getItem('name'); const nav = useNavigate(); return (<div className="bg-white shadow p-4 flex justify-between items-center"><div className="flex items-center gap-3"><div className="bg-teal-500 text-white rounded-lg w-10 h-10 flex items-center justify-center">🍏</div><div className="font-bold text-teal-700">FoodScan</div></div><div className="flex items-center gap-3">{name && <div className="text-teal-700 font-semibold">Hi, {name.split(' ')[0]}</div>}<Link to="/preferences" className="px-3 py-2 border rounded-md">Preferences</Link><Link to="/history" className="px-3 py-2 border rounded-md">History</Link><button onClick={()=>{localStorage.clear();nav('/login');}} className="bg-teal-700 text-white px-3 py-2 rounded-md">Logout</button></div></div>); }
